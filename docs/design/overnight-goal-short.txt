Build the rebuilt LTCM floor overnight so it can be switched on in the morning and run a week with no human in the loop. Work autonomously up to 8 hours; never stop to ask questions. First read ~/Work/long-term-capital-management/docs/design/overnight-goal.txt (the full goal; every line binds), docs/proposals/2026-09-19-the-game.md and docs/design/2026-09-19-architecture.md. Include my four additions (agent web search, research library, tool-request queue, graveyard playbook) and gpt-6-astra in all six roles.

DONE ALREADY, do not rebuild: the gateway (kalshi, alpaca, alpaca-paper, metered Astra route, caps), Coinbase removal, the five-section site, the $1,021.93 baseline.

KEEP YOUR PLACE: before any code create docs/runs/2026-09-20-overnight-build.md and keep it current: step, what is verified, what is next, each decision and why. Re-read it, the full goal and both design documents whenever you resume.

BUILD IN ORDER, verifying each step before the next; correctness beats coverage.
1. Append-only hash-chained ledger; one netting book per venue so agents never trade each other; Kalshi and Alpaca via the gateway; existing risk rules kept; runtime wired to alpaca-paper.
2. Evaluator rungs 0-1: mechanical replay, every backtest counted as a trial with an overfitting correction; forward tests on Alpaca paper and a conservative Kalshi shadow book; promotion and death by pre-registered tests on a lower confidence bound of after-cost log growth.
3. Economy and lifecycle on Sail: per-agent cost attribution, compute credits, niche budget floors, death at zero credits, forking for the rich, sleeping boxes with scheduled wakes, about a dozen seed agents from the old run's evidence plus Alpaca seeds, each with the four additions.
4. Rung 2 micro-real ($1-$10 positions) behind the Astra auditor, every rejection scored as if taken; publisher feeding the site's five sections.
5. Astra's other roles by pull request only: full tests plus replay regression, canary first, watchdog auto-rollback.
6. Rung 3 quarter-Kelly on the lower bound, drift monitors that demote decay, a standing evidence-ranked capital recommendation.

CONSTITUTION, unchangeable by any model or code on Sail: gateway order caps, the $100 Sail and $100 OpenAI monthly budgets, kill switch, ledger integrity, promotion and death thresholds. Venue and OpenAI keys live only in the gateway; agents hold only a House token.

HARD RULES: no real-money order tonight; kill switch engaged, floor loop stopped. Paper agents are held to the live account's limits (about $800, no leverage, no shorts, whole option contracts, $75 cap). Tonight's Sail and OpenAI spend each under $10; pause every box you resume. Never chain a deploy on a filter's exit status. Every module ships with tests; fix root causes, never skip, weaken or delete a test. Small pushed commits, no history rewrites. Never delete /workspace/.archive or the retired desk folder. Equities are closed until Monday: forward-test on Alpaca crypto and Kalshi hourlies, verify equity and option paths with accepted-then-cancelled paper orders, and report unfilled paths.

DONE WHEN, with evidence: (a) runtime, gateway and site suites pass, counts stated; (b) end-to-end dry run: agents wake, research, propose, net, fill on paper, ledger reconciles to the cent, credits move, one death and one fork by rule; (c) auditor and architect each make a real metered Astra call, a bad candidate is rejected, a broken pull request fails CI; (d) watchdog rolls back a bad canary deploy; (e) site renders all five sections from a test tape, production tape left clean; (f) docs/runbook-go-live.md: switch-on commands, first hour, how to stop, week's cost; (g) report in the progress file: built, verified, deferred, risks, spend; (h) both repos clean and pushed, deployed code matches main, README and docs agree with the code, old record moved to docs/history, no secret committed. If short of time, steps 1-4 with all but (d) come first; list what remains.