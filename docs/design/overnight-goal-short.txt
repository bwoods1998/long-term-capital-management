Build the rebuilt LTCM floor overnight so it can be switched on in the morning and run a week with no human in the loop. Work autonomously for up to 8 hours; do not stop to ask me questions. The full specification is ~/Work/long-term-capital-management/docs/design/overnight-goal.txt: read it first and treat every line of it as part of this goal (build order, constitution, hard rules, definition of done). Also follow docs/proposals/2026-09-19-the-game.md and docs/design/2026-09-19-architecture.md.

KEEP YOUR PLACE: before writing code create docs/runs/2026-09-20-overnight-build.md and keep it current (step, what is verified, what is next, every decision and why). Re-read it, the full goal file and both design documents whenever you resume.

BUILD IN ORDER, verifying each step before the next; correctness beats coverage: 1 ledger and per-venue netting book, wired to alpaca-paper; 2 evaluator rungs 0-1; 3 economy and agent lifecycle on Sail with the first dozen agents; 4 rung 2 micro-real behind the Astra auditor plus the site publisher; 5 Astra's other roles by pull request with canary and watchdog rollback; 6 rung 3 quarter-Kelly, drift monitors, capital recommendation.

HARD RULES: no real-money order tonight; the kill switch stays engaged and the floor loop stopped. Paper agents are held to the live account's real limits. Tonight's Sail spend under $10 and OpenAI under $10; pause every box you resumed. Never chain a deploy on a filter's exit status. Every module ships with tests; never skip, weaken or delete a test to get green. Small commits, pushed, no history rewrites. Never delete the archives or the retired desk folder. The constitution (gateway caps, the two $100 monthly budgets, kill switch, ledger integrity, promotion and death thresholds) is unchangeable by any model or code on Sail; keys stay only in the gateway.

DONE WHEN all are true with evidence shown: (a) runtime, gateway and site suites pass, counts stated; (b) end-to-end dry run on Alpaca paper and the Kalshi shadow book, ledger reconciled to the cent, credits move, one death and one fork by rule; (c) auditor and architect each make one real metered Astra call, a bad candidate is rejected, a broken pull request is refused by CI; (d) the watchdog rolls back a bad canary deploy; (e) the site renders all five sections from a test tape and the production tape is left clean; (f) docs/runbook-go-live.md exists; (g) the written report is in the progress file with tonight's spend and every known risk; (h) both repositories are clean, pushed, deployed code matches main, all documents agree with the code, no secret in any commit. If time runs short, steps 1-4 with (a)(b)(c)(e)(f)(g)(h) come first; list exactly what remains of 5 and 6.
