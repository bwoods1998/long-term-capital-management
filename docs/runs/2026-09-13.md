# First sustained portfolio run · September 13, 2026

**Question:** can repeated, source-checked research improve an investable S&P 500 portfolio, and which Sail capabilities help?

The cloud coordinator began at **09:10 Pacific**, with a fixed **14:10** deadline. See the [live portfolio](https://blakewoods.us/portfolio/) for current status. This page records the launch, not a completed five-hour result.

- Frozen SEC facts for all 503 securities; additional annual-filing retrieval retains source hashes.
- Adaptive research across DeepSeek V4 Pro, Kimi K2.6 and GLM 5.3; Kimi K3 critiques proposed allocations.
- Matched completion-window, memory and cache comparisons. Two isolated Sailbox forks compare full-universe and selected context.
- $90 inference ceiling, including $4 reserved for forks; separate $3.50 cloud ceiling. Actual charges, unresolved reservations and cloud costs are recorded separately.

The coordinator preserves requests and results, revisits unresolved questions and publishes a checkpoint every minute. A finished task queue does not end the run. A local watchdog recovers the same cloud process and preserves final journals before sleeping the host.

Readiness checks verified restricted credential injection, denied unrelated routes, same-disk sleep/wake and process recovery, restored SQLite backups, actual SEC retrieval from the Sailbox, delivered Voyage events and public checkpoint updates. Active runtime tests and GitHub CI pass.

The first fork launch stopped before inference: the cloned filesystem and host allowlist survived, but its HTTP credential binding did not. The controller now explicitly binds the reviewed policy on each child. Both unused resources were terminated; the failed launch cost $0.010004266. A 09:37 retry reuses the original $4 reservation, with its own resource and recovery records. The coordinator continued throughout.

Cloudflare accepted the email connection test at 09:32, and inbox delivery was confirmed. Completion alerts use the actual terminal status; a separate alarm detects missing updates.

The paper account starts with $100,000 of virtual cash. Sunday research can produce a pending allocation; it cannot produce a regular-session fill. No investment performance is claimed.

**Results to assess:** coverage and source errors; changes after criticism; matched quality, latency and cost; observed cache reuse; branch outcomes; interruptions and unsettled usage. More spending alone is not an improvement.

[Evaluation protocol](../EVALUATION.md) · [Architecture](../ARCHITECTURE.md) · [Operations](../OPERATIONS.md)
